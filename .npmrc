# Create React App + ESLint load plugins from multiple config paths under pnpm's
# default symlinked layout, which triggers "Plugin react was conflicted".
# Hoisted linker uses a flat node_modules (npm-like) so one copy resolves.
node-linker=hoisted
